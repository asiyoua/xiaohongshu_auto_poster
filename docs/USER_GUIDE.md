# bxz-xhs 用户指南

## 快速开始

只需说：**"小红书图片" + 你的内容主题**

例如：
- "小红书图片：AI 小白到 AI 牛人的职业转型路径，真实故事分享"
- "小红书图片：帮我整理这几款面霜的测评"
- "小红书图片：分享我的学习笔记"

---

## ⚠️ 首次使用前必读

### 1. 创建输出目录

**重要**：你需要告诉系统把生成的图片保存在哪里。

**默认位置**：`~/Myxhs/`

**自定义位置**：在 EXTEND.md 配置文件中设置：

```bash
# 创建配置文件
mkdir -p ~/.baoyu-skills/bxz-xhs
cat > ~/.baoyu-skills/bxz-xhs/EXTEND.md << 'EOF'
---
output_dir: ~/Documents/xhs-images  # 改成你想要的路径
---
EOF
```

**目录创建命令**：
```bash
# 使用默认位置
mkdir -p ~/Myxhs

# 或使用自定义位置
mkdir -p ~/Documents/xhs-images
```

### 2. 配置 API Key

图片生成需要 Gemini Nano Banana API Key：

```bash
# 创建配置目录
mkdir -p ~/.config/bxz-xhs

# 创建配置文件（替换 YOUR_API_KEY_HERE 为你的实际 key）
cat > ~/.config/bxz-xhs/config.ini << 'EOF'
[gemini_nano]
api_key=YOUR_API_KEY_HERE
EOF
```

**获取 API Key**：访问 Gemini Nano Banana API 平台获取你的 API Key

### 3. 检查配置

```bash
# 查看输出目录配置
cat ~/.baoyu-skills/bxz-xhs/EXTEND.md

# 查看 API Key 配置
cat ~/.config/bxz-xhs/config.ini

# 确认输出目录存在
ls -la ~/Myxhs  # 或你的自定义路径
```

---

## 一、输入材料说明

### 方式 1：直接描述内容（推荐）

直接告诉 Claude 你想要的内容主题和风格：

```
小红书图片：AI 职业转型，真实故事分享的感觉，用真人的口吻
```

### 方式 2：提供现有文件

如果你已经准备好素材文件：

**文件命名规则**（用于自动识别）：

| 类型 | 识别规则 | 示例文件名 |
|------|---------|-----------|
| **发布文字** | 文件名包含 `source` | `source-article.md`<br>`my-source.txt`<br>`source_素材.docx` |
| **发布图片** | 输出目录下所有 `.png` 文件 | `01-cover.png`<br>`02-content.png`<br>`*.png` |

**识别优先级**（发布文字）：
1. 文件名包含 `source` 的文件
2. `source-article.md` 文件
3. `outline.md` 中的内容摘要

**识别规则**（发布图片）：
- 扫描整个会话目录
- 收集所有 `.png` 文件
- 按文件名排序（01、02、03...）

**支持的文件类型**：
- Markdown (`.md`)
- 纯文本 (`.txt`)
- Word 文档 (`.docx`)
- 图片文件 (`.png`, `.jpg`, `.webp`)

### 方式 3：直接粘贴内容

在对话中直接粘贴你的内容：

```
小红书图片：
[粘贴你的文章内容]
```

---

## 二、输出文件结构

### 默认输出位置

```
~/Myxhs/{主题slug}/
├── source-article.md              # 原始输入材料（自动保存）
├── analysis.md                    # 内容分析报告
├── outline-strategy-a.md          # 方案 A：故事驱动型
├── outline-strategy-b.md          # 方案 B：信息密集型
├── outline-strategy-c.md          # 方案 C：视觉优先型
├── outline.md                     # 最终选定的大纲
├── prompts/                       # 图片生成提示词
│   ├── 01-cover-{slug}.md
│   ├── 02-pain-point-{slug}.md
│   └── ...
├── prompts_batch.xlsx             # 批量生成 Excel 文件
├── 01-cover-{slug}.png            # 生成的图片 ← 用于发布
├── 02-pain-point-{slug}.png       # 生成的图片 ← 用于发布
└── ...
```

### 自定义输出位置

如果在 EXTEND.md 中配置了 `output_dir`，则输出到：

```
{你的output_dir}/{主题slug}/
├── （同上结构）
└── ...
```

### 文件说明

| 文件 | 用途 | 可编辑性 | 发布相关 |
|------|------|----------|----------|
| `source-article.md` | 原始输入材料 | ✅ 可编辑 | ✅ **发布文字来源** |
| `source-*.md` | 其他 source 文件 | ✅ 可编辑 | ✅ **优先用于发布** |
| `analysis.md` | AI 分析报告 | 📖 参考 | - |
| `outline-strategy-*.md` | 三种方案 | 📖 参考 | - |
| `outline.md` | 最终大纲 | ✅ 可编辑 | ✅ **标题来源** |
| `prompts/*.md` | 图片提示词 | ✅ 可编辑 | - |
| `*.png` | 生成的图片 | - | ✅ **发布图片** |

---

## 三、自动发布功能详解

### 发布内容识别规则

系统会自动从会话目录中提取发布内容：

#### 1. 标题提取

**来源**：`outline.md` 文件中第一张图（封面）的 `Hook` 字段

**示例**：
```yaml
## P1 Cover
**Type**: cover
**Hook**: "文科生零基础 → 3年薪资翻3倍"  ← 这就是标题
```

#### 2. 内容提取

**优先级顺序**：

```python
# 伪代码表示识别逻辑
发布内容 = None

# 优先级 1: 查找文件名包含 "source" 的文件
for file in 目录下所有文件:
    if "source" in file名:
        发布内容 = 读取文件内容
        break

# 优先级 2: 使用 source-article.md
if 发布内容 is None:
    if 存在("source-article.md"):
        发布内容 = 读取("source-article.md")

# 优先级 3: 从 outline.md 提取摘要
if 发布内容 is None:
    发布内容 = 提取大纲摘要("outline.md")
```

**文件名匹配规则**（包含 `source` 即可）：
- ✅ `source-article.md`
- ✅ `my-source.txt`
- ✅ `source_素材.docx`
- ✅ `backup-source.md`
- ❌ `article-source-backup.md`（也包含 source，会被识别）
- ❌ `article.md`（不包含 source）

#### 3. 图片提取

**识别规则**：

```python
# 伪代码表示识别逻辑
发布图片列表 = []

# 扫描整个会话目录
for file in 目录下所有文件:
    if file扩展名 == ".png":
        发布图片列表.append(文件路径)

# 按文件名排序
发布图片列表.sort()  # 01-cover.png, 02-..., 03-...
```

**文件名匹配规则**（只要是 `.png` 即可）：
- ✅ `01-cover.png`
- ✅ `02-content.png`
- ✅ `screenshot.png`
- ✅ `image.png`
- ❌ `image.jpg`（不是 png）

### 发布方式

#### 方式 1：生成后立即发布

在图片生成完成后，选择"全自动发布"：

```
是否立即发布到小红书？
→ 选择：Yes（全自动发布）
```

#### 方式 2：后续手动发布

```bash
# 切换到会话目录
cd ~/Myxhs/{主题slug}

# 或你的自定义路径
cd ~/Documents/xhs-images/{主题slug}

# 运行发布脚本
python3 ~/.claude/skills/bxz-xhs/scripts/auto_publish.py
```

### 发布前检查清单

- [ ] 已配置输出目录（`~/Myxhs` 或自定义路径）
- [ ] xiaohongshu-mcp 服务已启动
- [ ] 小红书账号已登录
- [ ] 图片已生成完成（`.png` 文件存在）
- [ ] 发布文字已准备（文件名含 `source` 或 `source-article.md`）

### 发布服务状态检查

```bash
# 1. 检查服务是否运行
ps aux | grep xiaohongshu-mcp

# 2. 检查登录状态
cd ~/xiaohongshu-mcp
./xiaohongshu-login
# 应显示：当前登录状态: true

# 3. 如果未登录，重新登录
./xiaohongshu-login
# 会打开浏览器，扫码或账号密码登录

# 4. 登录成功后启动服务（如果还没启动）
./xiaohongshu-mcp -headless=false
# 服务将在 http://localhost:18060 运行

# 5. 查看服务日志（可选）
tail -f /tmp/xiaohongshu-mcp.log
```

---

## 四、修改已生成的内容

### 修改单张图片

1. 编辑对应的提示词文件 `prompts/NN-{type}-{slug}.md`
2. 重新生成图片（保留相同的 session ID 确保风格一致）

### 修改发布文字

**方式 1**：编辑现有的 source 文件
```bash
vim ~/Myxhs/{主题}/source-article.md
```

**方式 2**：创建新的 source 文件（会被优先识别）
```bash
# 创建新的 source 文件
vim ~/Myxhs/{主题}/source-updated.md
```

### 修改标题

编辑 `outline.md`，修改封面页的 `Hook` 字段：

```yaml
## P1 Cover
**Type**: cover
**Hook**: "新标题在这里"  ← 修改这里
```

### 添加新图片

1. 在 `prompts/` 目录创建新的提示词文件
2. 使用正确的序号（如 `09-extra-{slug}.md`）
3. 生成图片后，后续图片序号自动顺延

### 删除图片

1. 删除对应的 `.png` 文件和 `prompts/*.md` 文件
2. 后续图片序号需要手动重命名

---

## 五、常见问题

### Q: 如何修改输出目录？

**A**: 编辑 EXTEND.md 配置文件：

```bash
# 用户级配置
vim ~/.baoyu-skills/bxz-xhs/EXTEND.md

# 添加或修改
---
output_dir: ~/Documents/my-xhs-images
---

# 创建目录
mkdir -p ~/Documents/my-xhs-images
```

### Q: 图片生成失败了怎么办？

**A**: 检查 API Key 配置：

```bash
# 查看配置
cat ~/.config/bxz-xhs/config.ini

# 查看配置文件内容
cat ~/.config/bxz-xhs/config.ini

# 重新配置
mkdir -p ~/.config/bxz-xhs
cat > ~/.config/bxz-xhs/config.ini << EOF
[gemini_nano]
api_key=YOUR_API_KEY_HERE
EOF
```

### Q: 如何使用自己的素材图片？

**A**: 将图片复制到会话目录：

```bash
# 默认位置
cp /path/to/your/image.png ~/Myxhs/{主题slug}/

# 自定义位置
cp /path/to/your/image.png ~/Documents/xhs-images/{主题slug}/
```

### Q: 发布失败了怎么办？

**A**: 检查以下几点：

1. **服务状态**：`ps aux | grep xiaohongshu-mcp`
2. **服务日志**：`tail -f /tmp/xiaohongshu-mcp.log`
3. **登录状态**：`~/xiaohongshu-mcp/xiaohongshu-login`
4. **图片路径**：确认 `.png` 文件在会话目录下
5. **文字文件**：确认有文件名包含 `source` 的文件

### Q: 我的文字文件为什么没被识别？

**A**: 检查文件名是否包含 `source`：

```bash
# 列出会话目录所有文件
ls -la ~/Myxhs/{主题}/

# 确保有包含 "source" 的文件
# 例如：source-article.md ✅
#      article.md ❌
```

### Q: 可以修改发布的内容吗？

**A**: 可以，编辑以下文件：
- **标题**：修改 `outline.md` 中封面页的 Hook
- **内容**：修改 `source-article.md` 或创建新的 `source-*.md` 文件
- **图片**：修改对应的 `.png` 文件

---

## 六、风格和布局参考

### 推荐搭配

| 内容类型 | 推荐风格 | 推荐布局 |
|---------|---------|---------|
| 故事分享 | warm / cute | balanced |
| 知识干货 | notion / study-notes | dense / list |
| 产品测评 | bold / minimal | comparison |
| 教程指南 | chalkboard | flow |
| 氛围感内容 | pop / retro | sparse |

### 预览风格选择

系统会根据你的内容生成 3 张实时预览图，帮你选择最合适的风格。

---

## 七、配置文件说明

### EXTEND.md（可选配置）

用于保存个人偏好配置：

```yaml
---
# 输出目录配置
output_dir: ~/Myxhs  # 或其他路径

# 视觉偏好
watermark: false  # 是否添加水印
default_style: study-notes  # 默认风格

# 界面设置
language: zh  # 界面语言
---
```

配置位置（按优先级）：
1. 项目级：`.baoyu-skills/bxz-xhs/EXTEND.md`
2. 用户级：`~/.baoyu-skills/bxz-xhs/EXTEND.md`

### API Key 配置

```bash
# 推荐：用户级配置
mkdir -p ~/.config/bxz-xhs
cat > ~/.config/bxz-xhs/config.ini << EOF
[gemini_nano]
api_key=YOUR_API_KEY_HERE
EOF
```

---

## 八、完整工作流程示例

```
首次使用
  ↓
[配置] 创建输出目录 + 配置 API Key
  ↓
用户输入 "小红书图片：xxx"
  ↓
[Step 1] 内容分析 → analysis.md
  ↓
[Step 2] 确认理解（核心卖点、受众、风格）
  ↓
[Step 3] 生成三种方案 → outline-strategy-a/b/c.md
  ↓
[Step 4] 选择方案 + 风格预览 → outline.md
  ↓
[Step 5] 批量生成图片 → 8 张 PNG
  ↓
[Step 6] 完成报告
  ↓
[Step 7] 是否发布？
  ├─ Yes → 检测 source 文件 + *.png → 自动发布
  └─ No → 保存到 ~/Myxhs/
```

---

## 九、给其他使用者的建议

1. **首次使用前**：务必先创建输出目录和配置 API Key
2. **内容准备**：提前准备好文字素材，可以直接粘贴
3. **文件命名**：发布文字文件名要包含 `source`
4. **风格选择**：利用实时预览功能，选择最适合的风格
5. **保存习惯**：每个主题会独立保存，便于管理和复用
6. **发布前检查**：确认标题、内容、图片都符合预期再发布

---

## 十、配置检查清单

使用前请确认以下配置：

```bash
# 1. 输出目录
ls -la ~/Myxhs  # 或你的自定义路径

# 2. Gemini Nano Banana API Key
cat ~/.config/bxz-xhs/config.ini

# 3. 配置文件（可选）
cat ~/.baoyu-skills/bxz-xhs/EXTEND.md

# 4. 发布服务检查（如需发布）
cd ~/xiaohongshu-mcp

# 4.1 检查登录状态
./xiaohongshu-login
# 应显示：当前登录状态: true

# 4.2 如果未登录，执行登录
./xiaohongshu-login
# 会打开浏览器，扫码或账号密码登录小红书

# 4.3 登录成功后，启动服务
./xiaohongshu-mcp -headless=false &
# 服务将在后台运行，地址：http://localhost:18060
```

**快速发布流程**：
1. 先用 `xiaohongshu-login` 登录小红书
2. 再用 `xiaohongshu-mcp -headless=false` 启动发布服务
3. 最后运行 `auto_publish.py` 自动发布
